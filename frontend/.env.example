# Vite exposes variables prefixed with VITE_ to the browser bundle.
# The API proxy in vite.config.ts forwards /api → http://localhost:4000
# so no VITE_API_URL is needed in development.
# For production builds that don't use the proxy, set this:
# VITE_API_URL=https://your-backend.example.com
