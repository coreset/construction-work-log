PORT=4000
FRONTEND_URL=http://localhost:5173

# PostgreSQL connection string
DATABASE_URL=postgres://worklog:worklog@localhost:5432/worklog

# Set to "true" for hosted databases that require SSL (Neon, Supabase, RDS, etc.)
DB_SSL=false

# Set to "false" in production — use migrations instead
DB_SYNCHRONIZE=true
